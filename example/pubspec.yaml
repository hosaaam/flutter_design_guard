name: flutter_design_guard_example
description: Example usage of Flutter Design Guard diagnostics.
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
